# PPC Control Dashboard

Cross-portfolio control surface for paul-pc — projects, scheduled jobs, ClaudeBridge bus state.

`state.json` is regenerated by the `dashboard-publish` scheduled task on PPC and pushed to this repo. `index.html` reads `state.json` on load.

Live: https://gonzo8285.github.io/ppc-dashboard/
